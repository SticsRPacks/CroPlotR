% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/specific_plotting_dynamic.R
\name{base_dyn_plot}
\alias{base_dyn_plot}
\title{Builds a base dynamic plot showing simulated values as a line
and, when available, observed values as points with optional error bars.}
\usage{
base_dyn_plot(df_data, title, extra_aes = NULL, extra_obs_aes = NULL)
}
\arguments{
\item{df_data}{Data frame containing at least a \code{Date} and a \code{Simulated}
column. If an \code{Observed} column is present, observed values are overlaid as
points. If an \code{Obs_SD} column is also present, error bars (±2 SD) are
added.}

\item{title}{String. Title displayed at the top of the plot.}

\item{extra_aes}{Optional named list of additional aesthetics merged into
the base plot mapping (e.g. colour, group). Defaults to NULL.}

\item{extra_obs_aes}{Optional named list of additional aesthetics merged into
the observed points mapping. Defaults to NULL.}
}
\value{
A ggplot object representing the time series plot.
}
\description{
Builds a base dynamic plot showing simulated values as a line
and, when available, observed values as points with optional error bars.
}
\keyword{internal}
